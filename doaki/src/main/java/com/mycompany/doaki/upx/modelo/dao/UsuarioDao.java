
package com.mycompany.doaki.upx.modelo.dao;
 import com.mycompany.doaki.upx.modelo.dominio.*;

public class UsuarioDao {
    
    /* continuar video 11 daqui!!
    private final Conexao conexao;
    
    public UsuarioDao(){
        this.conexao = new ConexaoMysql();
    }
    
 
    public String salvar(Usuario usuario){ //usuario dando erro aaaa
        return usuario.getId()== 0 ? adicionar(usuario): editar(usuario);
    }

    private String adicionar(Usuario usuario) {
        String sql = "INSERT INTO usuario (nome,usuario,senha,perfil,estado)VALUES (?,?,?,?,?)";

    }

    private String editar(Usuario usuario) {
        String sql = "UPDATE usuario SET nome = ?, usuario = ?, senha=?, perfil = ?, estado =? 2WHERE id=?";
    } */
}
